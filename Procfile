# =============================================================================
# HEROKU PROCFILE - CARAFE COFFEE
# =============================================================================
# Defines process types for Heroku deployment
# Note: For multi-app deployment, you'll need separate Heroku apps for
# frontend and backend, each with their own Procfile

# Default: Run the startup script that manages both services
web: npm start
