╔════════════════════════════════════════════════════════════════════════╗
║                    HEROKU DEPLOYMENT QUICK START                       ║
║                         Carafe Coffee Project                          ║
╚════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────┐
│ PREREQUISITES                                                          │
└────────────────────────────────────────────────────────────────────────┘

  1. Install Heroku CLI:
     $ brew install heroku/brew/heroku

  2. Login to Heroku:
     $ heroku login

  3. Commit all changes:
     $ git status && git add . && git commit -m "Ready for Heroku"

┌────────────────────────────────────────────────────────────────────────┐
│ DEPLOYMENT OPTIONS                                                     │
└────────────────────────────────────────────────────────────────────────┘

  ┌─ OPTION 1: AUTOMATED (RECOMMENDED) ─────────────────────────────┐
  │                                                                  │
  │  $ ./scripts/generate-secrets.sh                                │
  │  $ ./scripts/heroku-deploy.sh both                              │
  │                                                                  │
  │  Follow the interactive prompts!                                │
  └──────────────────────────────────────────────────────────────────┘

  ┌─ OPTION 2: MANUAL BACKEND ──────────────────────────────────────┐
  │                                                                  │
  │  $ heroku create carafe-strapi                                  │
  │  $ heroku addons:create heroku-postgresql:mini \                │
  │      --app carafe-strapi                                        │
  │  $ ./scripts/generate-secrets.sh                                │
  │  $ # Set config vars from generate-secrets.sh output            │
  │  $ git remote add heroku-backend \                              │
  │      https://git.heroku.com/carafe-strapi.git                   │
  │  $ git subtree push --prefix strapi-backend \                   │
  │      heroku-backend master                                      │
  └──────────────────────────────────────────────────────────────────┘

  ┌─ OPTION 3: MANUAL FRONTEND ─────────────────────────────────────┐
  │                                                                  │
  │  $ heroku create carafe-frontend                                │
  │  $ heroku config:set \                                          │
  │      NUXT_PUBLIC_SITE_URL=https://carafe-frontend.herokuapp.com \│
  │      NUXT_PUBLIC_STRAPI_URL=https://carafe-strapi.herokuapp.com \│
  │      STRAPI_API_TOKEN=<from-strapi-admin> \                     │
  │      --app carafe-frontend                                      │
  │  $ git remote add heroku-frontend \                             │
  │      https://git.heroku.com/carafe-frontend.git                 │
  │  $ git subtree push --prefix nuxt-frontend \                    │
  │      heroku-frontend master                                     │
  └──────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│ POST-DEPLOYMENT                                                        │
└────────────────────────────────────────────────────────────────────────┘

  1. Create Strapi Admin:
     → Open: https://carafe-strapi.herokuapp.com/admin
     → Create first admin user

  2. Generate API Token:
     → Settings → API Tokens → Create new token
     → Copy token

  3. Set Frontend Token:
     $ heroku config:set STRAPI_API_TOKEN=<token> \
         --app carafe-frontend

  4. Restart Frontend:
     $ heroku restart --app carafe-frontend

  5. Test Everything:
     → Backend: https://carafe-strapi.herokuapp.com
     → Frontend: https://carafe-frontend.herokuapp.com

┌────────────────────────────────────────────────────────────────────────┐
│ MONITORING                                                             │
└────────────────────────────────────────────────────────────────────────┘

  # View logs (live)
  $ heroku logs --tail --app carafe-strapi
  $ heroku logs --tail --app carafe-frontend

  # Check app status
  $ heroku ps --app carafe-strapi
  $ heroku ps --app carafe-frontend

  # View config
  $ heroku config --app carafe-strapi
  $ heroku config --app carafe-frontend

  # Restart apps
  $ heroku restart --app carafe-strapi
  $ heroku restart --app carafe-frontend

┌────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                        │
└────────────────────────────────────────────────────────────────────────┘

  Build failed?
  → Check logs: heroku logs --tail --app <app-name>
  → Verify package.json engines field
  → Check for missing dependencies

  Database connection failed?
  → Verify DATABASE_URL: heroku config --app carafe-strapi
  → Check database status: heroku pg:info --app carafe-strapi

  Frontend can't reach backend?
  → Verify NUXT_PUBLIC_STRAPI_URL is correct
  → Check CORS settings in Strapi
  → Verify STRAPI_API_TOKEN is set

  App crashed?
  → View crash logs: heroku logs --tail --app <app-name>
  → Check dyno status: heroku ps --app <app-name>
  → Restart: heroku restart --app <app-name>

┌────────────────────────────────────────────────────────────────────────┐
│ USEFUL RESOURCES                                                       │
└────────────────────────────────────────────────────────────────────────┘

  📖 Full Guide:       HEROKU_DEPLOYMENT.md
  ✅ Checklist:        HEROKU_CHECKLIST.md
  📋 Files Summary:    HEROKU_FILES_SUMMARY.md
  🔧 Scripts Guide:    scripts/README.md

  🌐 Heroku Docs:      https://devcenter.heroku.com
  📊 Status Page:      https://status.heroku.com
  💬 Support:          https://help.heroku.com

┌────────────────────────────────────────────────────────────────────────┐
│ COST ESTIMATE (2026 Pricing)                                          │
└────────────────────────────────────────────────────────────────────────┘

  Eco Dynos (1 per app):          $5/month × 2 = $10/month
  PostgreSQL Mini:                                $5/month
  ─────────────────────────────────────────────────────────
  TOTAL (Basic Setup):                           $15/month

  Standard-1X Dynos:              $25/month × 2 = $50/month
  PostgreSQL Mini:                                $5/month
  ─────────────────────────────────────────────────────────
  TOTAL (Production):                            $55/month

┌────────────────────────────────────────────────────────────────────────┐
│ EMERGENCY COMMANDS                                                     │
└────────────────────────────────────────────────────────────────────────┘

  # Rollback to previous release
  $ heroku rollback --app <app-name>

  # Scale down (emergency cost saving)
  $ heroku ps:scale web=0 --app <app-name>

  # Database backup
  $ heroku pg:backups:capture --app carafe-strapi

  # Reset database (CAUTION!)
  $ heroku pg:reset DATABASE_URL --app carafe-strapi \
      --confirm carafe-strapi

  # Open remote shell
  $ heroku run bash --app <app-name>

╔════════════════════════════════════════════════════════════════════════╗
║  Need help? Check the full documentation in HEROKU_DEPLOYMENT.md      ║
╚════════════════════════════════════════════════════════════════════════╝
